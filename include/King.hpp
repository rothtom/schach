#include "Piece.hpp"

namespace chess {
    class King: public Piece {
        public:
            King(color piece_color, ChessCoordinates coordinates, sf::Texture texture);
        private:
            
    };
}